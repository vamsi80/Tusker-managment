{
  "cli": {
    "version": ">= 5.9.1",
    "appVersionSource": "local"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "android": {
        "buildType": "app-bundle"
      }
    },
    "production": {},
    "apk": {
      "extends": "production",
      "android": {
        "buildType": "apk"
      },
      "//": "EAS uploads the project via git, and .env is gitignored, so EXPO_PUBLIC_* must be declared here or the build bakes in undefined and the app falls back to localhost:4000. This LAN address only resolves on the dev machine's Wi-Fi with `pnpm dev:api` running - change it to the deployed API before sharing this APK with anyone.",
      "env": {
        "EXPO_PUBLIC_API_BASE": "http://192.168.1.44:4000"
      }
    },
    "ios-simulator": {
      "developmentClient": true,
      "ios": {
        "simulator": true
      }
    }
  },
  "submit": {
    "production": {}
  }
}