{
  "cli": {
    "version": ">= 5.9.1",
    "appVersionSource": "local"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "android": {
        "buildType": "app-bundle"
      }
    },
    "//": "EAS uploads the project via git, and .env is gitignored, so EXPO_PUBLIC_* must be declared here or the build bakes in undefined and the app falls back to localhost:4000. This value is inlined into the binary at build time and CANNOT be changed afterwards - moving to a custom domain later means rebuilding and redistributing every install.",
    "production": {
      "env": {
        "EXPO_PUBLIC_API_BASE": "https://REPLACE-WITH-VERCEL-DOMAIN.vercel.app"
      }
    },
    "apk": {
      "extends": "production",
      "android": {
        "buildType": "apk"
      }
    },
    "ios-simulator": {
      "developmentClient": true,
      "ios": {
        "simulator": true
      }
    }
  },
  "submit": {
    "production": {}
  }
}