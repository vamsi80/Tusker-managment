// styled-jsx augments JSX with the `jsx`/`global` props used by <style jsx>.
// Next used to pull these in transitively; reference them explicitly so the
// hoisted node_modules layout cannot drop them.
/// <reference types="styled-jsx" />
