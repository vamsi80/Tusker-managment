{
  "framework": "nextjs",
  "regions": ["bom1"],
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm prisma generate && pnpm --filter @tusker/web run build",
  "outputDirectory": ".next"
}
